[request_definition]
r = input, obj, act

[policy_definition]
p = act, obj, op, p1, p2

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = r.act == p.act && regexMatch(r.obj, p.obj) && abacMatcher(r.input, p.op, p.p1, p.p2)

